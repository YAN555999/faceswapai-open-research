{
  "name": "faceswapai-open-research",
  "version": "1.2.0",
  "private": true,
  "description": "Version-pinned public mirror and integrity checks for the FaceSwapAI research and open-data catalog.",
  "license": "CC-BY-4.0",
  "type": "module",
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "test": "node scripts/verify.mjs && node scripts/build-pages.mjs && node scripts/verify-pages.mjs",
    "verify": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
